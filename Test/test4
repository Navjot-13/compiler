# Implementing our own mod function

int mod (int a, int b) {
    decimal d;
    d = a / b;
    int c;
    c = d;
    return a - (b*c);
}

begin {
    int a;
    a = input();
    int b;
    b = input();
    int y;
    y = mod(a,b);
    print(y);
}